import React, { FunctionComponent } from 'react';
import { Balance } from './balance/balance';

import './root.styl';

export const Root = () => {
    return (
        <div className="container">
        </div>
    );
};